apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: streaming-backend
spec:
  type: ClusterIP
  ports:
    - port: 5001
      targetPort: 5001
      protocol: TCP
  selector:
    app: streaming-backend
